// Technically this works if I import "felipepavanela78601.ipage.com" to the domains
// However, it fails Vercel's pipeline and app can't be deployed
{
    "rewrites": [
        {
            "source": "https://apis.pavanela.com/:path*",
            "destination": "https://felipepavanela78601.ipage.com/apis/:path*"
        },
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ]
}
